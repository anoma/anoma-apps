module Counter.Simple.Increment.Encoded;

import Stdlib.Prelude open;

-- After the counter is initialized set this variable to the output of:
--    make grpc_port=$GRPC_PORT get-unspent-resources
currentCounterResourceEncoded : Nat := 0;

-- After the counter is initialized set this variable to the output of:
--      make grpc_port=$GRPC_PORT get-latest-root
latestRoot : Nat := 0;
