module Kudos.KudosKind;

import Stdlib.Prelude open;
import Applib open;
import Anoma.Identity.External open;
import Anoma.Identity.Internal open;
import Kudos.Layer open;

main
  (kudosLogic : Logic)
  (originatorBs : ByteArray)
  (kudosSymbol : String)
  : Kind :=
  let
    originator := ExternalIdentity.fromByteArray originatorBs;
    identity :=
      Kudos.mkIdentity@{
        originator;
        kudosSymbol;
      };
  in computeKudosKind kudosLogic identity;
