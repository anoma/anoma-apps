module Kudos.Swap;

import Stdlib.Prelude open;
import Applib open;
import Anoma.Identity.External open;
import Anoma.Identity.Internal open;
import Kudos.Layer open;

main
  (privateKeyPayload publicKeyPayload : ByteArray)
  (rngSeed : Nat)
  (logic : Logic)
  (giveSymbol : String)
  (giveQuantity : Nat)
  (wantSymbol : String)
  (wantQuantity : Nat) : Transaction := todo;
